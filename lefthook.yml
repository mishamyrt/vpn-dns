pre-commit:
  parallel: true
  commands:
    golangci-lint:
      glob: "*.go"
      run: golangci-lint run {staged_files}
    revive-lint:
      glob: "*.go"
      run: revive -config revive.toml {staged_files}
    test:
      run: make test
